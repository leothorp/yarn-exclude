{
  "name": "yarn-exclude",
  "version": "0.0.18",
  "description": "Exclude a workspace from yarn install in a yarn monorepo.",
  "main": "src/cli.js",
  "bin": {
    "yarn-exclude": "bin/yarn-exclude.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/leothorp/yarn-exclude.git"
  },
  "keywords": [
    "yarn",
    "monorepo",
    "exclude",
    "workspace",
    "package"
  ],
  "scripts": {
    "publish": "./scripts/publish.sh patch",
    "test": "yarn cache clean && rm -rf ../test-mono/node_modules && node ./index.js --cwd ../test-mono --exclude three --modify"
  },
  "engines": {
    "node": ">=10"
  },
  "author": "leothorp",
  "license": "MIT",
  "dependencies": {
    "commander": "^7.1.0",
    "glob": "^7.1.6"
  }
}
