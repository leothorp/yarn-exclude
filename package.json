{
  "name": "yarn-exclude",
  "version": "0.0.1",
  "description": "Exclude a workspace from yarn install in a yarn monorepo.",
  "main": "src/cli.js",
  "bin": "bin/yarn-exclude.js",
  
  "keywords": [
    "yarn",
    "monorepo",
    "exclude",
    "workspace",
    "package"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./index.js --cwd ../test-mono --exclude three --modify",
    "test-run": "yarn cache clean && rm -rf ../test-mono/node_modules && node ./index.js --cwd ../test-mono --exclude three --modify"
  },
  "engines": {
    "node": ">=10"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^7.1.0",
    "glob": "^7.1.6"
  }
}
