{
  "name": "yarn-exclude",
  "version": "0.0.26",
  "description": "Exclude a workspace from yarn install in a yarn monorepo.",
  "main": "src/index.js",
  "files": [
    "src/*",
    "bin/*"
  ],
  "bin": {
    "yarn-exclude": "bin/yarn-exclude.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/leothorp/yarn-exclude.git"
  },
  "keywords": [
    "yarn",
    "monorepo",
    "exclude",
    "workspace",
    "package"
  ],
  "scripts": {
    "patch-version": "./scripts/publish.sh patch",
    "test": "node ./test/exclude.test.js"
  },
  "engines": {
    "node": ">=10"
  },
  "author": "leothorp",
  "license": "MIT",
  "dependencies": {
    "commander": "^7.1.0",
    "glob": "^7.1.6"
  },
  "devDependencies": {
    "shelljs": "^0.8.4"
  }
}
